<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Volatile" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <div class="h-screen flex flex-col overflow-hidden">
      <header class="flex-none bg-[#00FF88] px-4 py-2">
        <div class="flex items-center justify-between">
          <%= link_to [:root] do %>
            <%= icon("logo", size: "size-12") %>
          <% end %>

          <nav class="flex gap-4">
            <% if user_logged_in? %>
              <%= link_to [:account] do %>
                <%= avatar(user: Current.user, size: "size-10 border-2 border-white") %>
              <% end %>
            <% end %>
          </nav>
        </div>
      </header>

      <main class="flex-1 overflow-y-auto">
        <div class="p-4">
          <% flash.each do |key, value| %>
            <%= content_tag :div, value, class: "text-white px-4 py-2 mb-4 rounded #{flash_class(key)}" if value.present? %>
          <% end %>

          <%= yield %>
        </div>
      </main>

      <footer class="flex-none bg-white px-4 py-2">
        <nav class="flex items-center justify-between">
          <%= link_to [:root] do %>
            <%= icon("information-circle", size: "size-12", other: "text-black") %>
          <% end %>

          <%= button_to [:tracks], class: "cursor-pointer" do %>
            <%= icon("record-circle", size: "size-12", other: "text-black") %>
          <% end %>
        </nav>
      </footer>
    </div>
  </body>
</html>
